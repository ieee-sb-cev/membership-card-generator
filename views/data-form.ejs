<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Badge Data</title>
  </head>
  <style>
    body {
      display: grid;
      height: 100vh;
    }
    :is(submit, reset):hover {
      background-color: #fff;
      color: #000;
    }
  </style>
  <script src="https://cdn.tailwindcss.com"></script>
  <body class="bg-[hsl(230,93%,11%)] text-white">
    <div class="min-h-full min-w-screen flex justify-center items-center">
      <div
        class="flex flex-col items-center p-8 backdrop-blur-md bg-white bg-opacity-10 rounded-xl max-w-[720px] w-full"
      >
        <h1 class="font-bold text-3xl my-7">Card Generator</h1>
        <form
          action="/badge-gen"
          method="POST"
          class="grid grid-cols-2 items-center gap-8 gap-y-4 w-full"
        >
          <div>
            <div><label for="name">Full Name</label></div>
            <div>
              <input
                required
                class="w-full my-2 p-2 rounded-lg bg-gray-700"
                type="text"
                placeholder="Full Name"
                name="name"
                id="name"
              />
            </div>
          </div>

          <div>
            <div><label for="ieeenum">IEEE Membership Number</label></div>
            <div>
              <input
                required
                class="w-full my-2 p-2 rounded-lg bg-gray-700"
                type="text"
                placeholder="eg : 96472297"
                name="ieeenum"
                id="ieeenum"
              />
            </div>
          </div>

          <div class="w-full text-center">
            <input
              class="w-full py-2 font-medium rounded-lg bg-white text-black hover:bg-blue-500 hover:text-white"
              type="submit"
              value="Download Badge"
            />
          </div>
          <div class="w-full text-center">
            <!-- <input
              class="w-full py-2 font-medium rounded-lg bg-white text-black hover:bg-blue-500 hover:text-white"
              type="reset"
              value="Reset"
            /> -->
            <div
              class="w-full py-2 font-medium rounded-lg bg-white text-black hover:bg-blue-500 hover:text-white"
            >
              <a href="/">⬅ Back to Dashboard</a>
            </div>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
